<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- ============================================ -->
        <!-- TARJETAS EN EL HOME DEL PORTAL -->
        <!-- ============================================ -->
        <template id="portal_sat_home_extend" inherit_id="portal.portal_my_home" priority="20">
            <!-- Habilitar categoría de servicio -->
            <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
                <t t-set="portal_service_category_enable" t-value="True"/>
            </xpath>

            <!-- Tarjeta: Mis Equipos -->
            <xpath expr="//div[@id='portal_service_category']" position="inside">
                <t t-call="portal.portal_docs_entry">
                    <t t-set="title">Mis Equipos</t>
                    <t t-set="url" t-value="'/my/equipos'"/>
                    <t t-set="text">Consulte sus equipos en alquiler</t>
                    <t t-set="placeholder_count" t-value="'equipo_count'"/>
                </t>
            </xpath>

            <!-- Tarjeta: Mis Tickets -->
            <xpath expr="//div[@id='portal_service_category']" position="inside">
                <t t-call="portal.portal_docs_entry">
                    <t t-set="title">Mis Tickets</t>
                    <t t-set="url" t-value="'/my/tickets'"/>
                    <t t-set="text">Vea el estado de sus servicios técnicos</t>
                    <t t-set="placeholder_count" t-value="'ticket_count'"/>
                </t>
            </xpath>
        </template>

        <!-- ============================================ -->
        <!-- LISTA DE EQUIPOS -->
        <!-- ============================================ -->
        <!-- ============================================ -->
<!-- LISTA DE EQUIPOS - FORMATO TABLA -->
<!-- ============================================ -->
<template id="portal_my_equipos" name="Mis Equipos">
    <t t-call="portal.portal_layout">
        <t t-set="breadcrumbs_searchbar" t-value="True"/>

        <t t-call="portal.portal_searchbar">
            <t t-set="title">Mis Equipos</t>
        </t>

        <div class="container mt-4">
            <!-- Mensaje si no hay equipos -->
            <t t-if="not equipos">
                <div class="alert alert-info text-center" role="alert">
                    <i class="fa fa-info-circle fa-2x mb-3"></i>
                    <h4>No tiene equipos registrados</h4>
                    <p class="mb-0">Actualmente no tiene equipos en alquiler asociados a su cuenta.</p>
                </div>
            </t>

            <!-- Lista de equipos en formato tabla -->
            <t t-else="">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <div class="row align-items-center">
                            <div class="col-md-6">
                                <h5 class="mb-0">
                                    <i class="fa fa-print"></i> 
                                    Mis Equipos en Alquiler
                                </h5>
                            </div>
                            <div class="col-md-6 text-end">
                                <span class="badge bg-light text-dark fs-6">
                                    <t t-esc="len(equipos)"/> equipos
                                </span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="card-body p-0">
                        <t t-call="portal.portal_table">
                            <thead>
                                <tr class="active">
                                    <th>Serie</th>
                                    <th>Modelo</th>
                                    <th>Marca</th>
                                    <th>Tipo</th>
                                    <th>Ubicación</th>
                                    <th class="text-center">Contadores</th>
                                    <th>Estado</th>
                                    <th class="text-center">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-foreach="equipos" t-as="equipo">
                                    <tr>
                                        <!-- Serie -->
                                        <td>
                                            <strong>
                                                <span class="badge bg-dark">
                                                    <t t-esc="equipo.serie"/>
                                                </span>
                                            </strong>
                                        </td>
                                        
                                        <!-- Modelo -->
                                        <td>
                                            <div class="fw-bold">
                                                <i class="fa fa-print text-primary me-1"></i>
                                                <t t-esc="equipo.name.name"/>
                                            </div>
                                        </td>
                                        
                                        <!-- Marca -->
                                        <td>
                                            <t t-esc="equipo.marca"/>
                                        </td>
                                        
                                        <!-- Tipo -->
                                        <td>
                                            <t t-if="equipo.tipo_maquina_id == 'color'">
                                                <span class="badge bg-primary">
                                                    <i class="fa fa-palette"></i> Color
                                                </span>
                                            </t>
                                            <t t-else="">
                                                <span class="badge bg-secondary">
                                                    <i class="fa fa-circle"></i> B/N
                                                </span>
                                            </t>
                                        </td>
                                        
                                        <!-- Ubicación -->
                                        <td>
                                            <t t-if="equipo.direccion">
                                                <small class="text-muted">
                                                    <i class="fa fa-map-marker-alt text-danger me-1"></i>
                                                    <t t-esc="equipo.direccion[:40]"/>
                                                    <t t-if="len(equipo.direccion) > 40">...</t>
                                                </small>
                                            </t>
                                            <t t-else="">
                                                <span class="text-muted">-</span>
                                            </t>
                                        </td>
                                        
                                        <!-- Contadores -->
                                        <td class="text-center">
                                            <t t-if="equipo.contador_bn or equipo.contador_color">
                                                <div class="d-flex justify-content-center gap-2">
                                                    <!-- Contador B/N -->
                                                    <div class="small">
                                                        <div class="text-muted" style="font-size: 0.7rem;">B/N</div>
                                                        <div class="fw-bold">
                                                            <t t-esc="'{:,}'.format(equipo.contador_bn or 0)"/>
                                                        </div>
                                                    </div>
                                                    
                                                    <!-- Contador Color (solo si es color) -->
                                                    <t t-if="equipo.tipo_maquina_id == 'color'">
                                                        <div class="small">
                                                            <div class="text-muted" style="font-size: 0.7rem;">Color</div>
                                                            <div class="fw-bold text-primary">
                                                                <t t-esc="'{:,}'.format(equipo.contador_color or 0)"/>
                                                            </div>
                                                        </div>
                                                    </t>
                                                </div>
                                                
                                                <!-- Fecha de actualización -->
                                                <t t-if="equipo.fecha_ultima_actualizacion">
                                                    <div class="text-muted" style="font-size: 0.65rem; margin-top: 2px;">
                                                        <i class="fa fa-clock"></i>
                                                        <t t-esc="equipo.fecha_ultima_actualizacion.strftime('%d/%m/%y')"/>
                                                    </div>
                                                </t>
                                            </t>
                                            <t t-else="">
                                                <span class="text-muted">Sin datos</span>
                                            </t>
                                        </td>
                                        
                                        <!-- Estado -->
                                        <td>
                                            <t t-if="equipo.estado_alquiler_id == 'alquilada'">
                                                <span class="badge bg-success">
                                                    <i class="fa fa-check-circle"></i> Alquilado
                                                </span>
                                            </t>
                                            <t t-elif="equipo.estado_alquiler_id == 'con_problemas'">
                                                <span class="badge bg-danger">
                                                    <i class="fa fa-exclamation-triangle"></i> Con Problemas
                                                </span>
                                            </t>
                                            <t t-elif="equipo.estado_alquiler_id == 'lista'">
                                                <span class="badge bg-info">
                                                    <i class="fa fa-thumbs-up"></i> Listo
                                                </span>
                                            </t>
                                            <t t-else="">
                                                <span class="badge bg-secondary">
                                                    <t t-esc="dict(equipo._fields['estado_alquiler_id'].selection).get(equipo.estado_alquiler_id, equipo.estado_alquiler_id)"/>
                                                </span>
                                            </t>
                                        </td>
                                        
                                        <!-- Acciones -->
                                        <td class="text-center">
                                            <div class="btn-group btn-group-sm" role="group">
                                                <!-- Ver Detalle -->
                                                <a t-attf-href="/my/equipo/#{equipo.id}" 
                                                   class="btn btn-primary"
                                                   title="Ver Detalles">
                                                    <i class="fa fa-eye"></i>
                                                </a>
                                                
                                                <!-- Reportar Problema -->
                                                <a t-attf-href="/ticket/reportar_incidencia?id_registro=#{equipo.id}" 
                                                   class="btn btn-danger"
                                                   title="Reportar Problema">
                                                    <i class="fa fa-exclamation-circle"></i>
                                                </a>
                                                
                                                <!-- Solicitar Tóner -->
                                                <a t-attf-href="/toner/solicitar_toner?id_registro=#{equipo.id}" 
                                                   class="btn btn-info"
                                                   title="Solicitar Tóner">
                                                    <i class="fa fa-tint"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                </t>
                            </tbody>
                        </t>
                    </div>
                </div>
            </t>

            <!-- Paginación -->
            <t t-if="pager" t-call="portal.pager"/>
        </div>
    </t>
</template>

        <!-- ============================================ -->
        <!-- DETALLE DE EQUIPO -->
        <!-- ============================================ -->
        <template id="portal_equipo_detail" name="Detalle de Equipo">
            <t t-call="portal.portal_layout">
                <t t-set="o_portal_fullwidth_alert" groups="base.group_portal">
                    <t t-call="portal.portal_back_in_edit_mode">
                        <t t-set="backend_url" t-value="'/web#model=alquiler&amp;id=%s&amp;view_type=form' % (equipo.id)"/>
                    </t>
                </t>

                <div class="container mt-4 mb-5">
                    <!-- Breadcrumb -->
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/my/home">Portal</a></li>
                            <li class="breadcrumb-item"><a href="/my/equipos">Mis Equipos</a></li>
                            <li class="breadcrumb-item active" aria-current="page">
                                <t t-esc="equipo.serie"/>
                            </li>
                        </ol>
                    </nav>

                    <!-- Header -->
                    <div class="row mb-4">
                        <div class="col-12">
                            <div class="card border-primary shadow">
                                <div class="card-body">
                                    <div class="row align-items-center">
                                        <div class="col-md-8">
                                            <h2 class="mb-2">
                                                <i class="fa fa-print text-primary"></i>
                                                <t t-esc="equipo.name.name"/>
                                            </h2>
                                            <p class="text-muted mb-0">
                                                <strong>Serie:</strong>
                                                <span class="badge bg-dark fs-6 ms-2">
                                                    <t t-esc="equipo.serie"/>
                                                </span>
                                            </p>
                                        </div>
                                        <div class="col-md-4 text-md-end mt-3 mt-md-0">
                                            <t t-if="equipo.estado_alquiler_id == 'alquilada'">
                                                <span class="badge bg-success fs-5">Alquilado</span>
                                            </t>
                                            <t t-elif="equipo.estado_alquiler_id == 'con_problemas'">
                                                <span class="badge bg-danger fs-5">Con Problemas</span>
                                            </t>
                                            <t t-else="">
                                                <span class="badge bg-secondary fs-5">
                                                    <t t-esc="dict(equipo._fields['estado_alquiler_id'].selection).get(equipo.estado_alquiler_id, equipo.estado_alquiler_id)"/>
                                                </span>
                                            </t>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Información General -->
                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <div class="card h-100 shadow-sm">
                                <div class="card-header bg-primary text-white">
                                    <i class="fa fa-info-circle"></i> Información General
                                </div>
                                <div class="card-body">
                                    <table class="table table-sm table-borderless">
                                        <tr>
                                            <td class="text-muted">Modelo:</td>
                                            <td><strong><t t-esc="equipo.name.name"/></strong></td>
                                        </tr>
                                        <tr>
                                            <td class="text-muted">Marca:</td>
                                            <td><strong><t t-esc="equipo.marca"/></strong></td>
                                        </tr>
                                        <tr>
                                            <td class="text-muted">Serie:</td>
                                            <td><span class="badge bg-dark"><t t-esc="equipo.serie"/></span></td>
                                        </tr>
                                        <tr>
                                            <td class="text-muted">Tipo:</td>
                                            <td>
                                                <t t-if="equipo.tipo_maquina_id == 'color'">
                                                    <span class="badge bg-primary">Color</span>
                                                </t>
                                                <t t-else="">
                                                    <span class="badge bg-secondary">Monocromática</span>
                                                </t>
                                            </td>
                                        </tr>
                                    </table>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6 mb-3">
                            <div class="card h-100 shadow-sm">
                                <div class="card-header bg-info text-white">
                                    <i class="fa fa-map-marker-alt"></i> Ubicación
                                </div>
                                <div class="card-body">
                                    <t t-if="equipo.direccion">
                                        <p class="mb-2">
                                            <i class="fa fa-building text-primary"></i>
                                            <strong> Dirección:</strong>
                                        </p>
                                        <p class="ms-4"><t t-esc="equipo.direccion"/></p>
                                    </t>
                                    <t t-if="equipo.contacto_id">
                                        <p class="mb-1">
                                            <i class="fa fa-user text-success"></i>
                                            <strong> Contacto:</strong> <t t-esc="equipo.contacto_id"/>
                                        </p>
                                    </t>
                                    <t t-if="equipo.celular">
                                        <p class="mb-1">
                                            <i class="fa fa-phone text-warning"></i>
                                            <strong> Teléfono:</strong> <t t-esc="equipo.celular"/>
                                        </p>
                                    </t>
                                    <t t-if="equipo.correo_">
                                        <p class="mb-0">
                                            <i class="fa fa-envelope text-danger"></i>
                                            <strong> Email:</strong> <t t-esc="equipo.correo_"/>
                                        </p>
                                    </t>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Contadores Actuales -->
                    <t t-if="equipo.contador_bn or equipo.contador_color or equipo.contador_scan">
                        <div class="row mb-4">
                            <div class="col-12">
                                <div class="card shadow-sm">
                                    <div class="card-header bg-success text-white">
                                        <i class="fa fa-tachometer-alt"></i> Contadores Actuales
                                        <t t-if="equipo.fecha_ultima_actualizacion">
                                            <small class="float-end">
                                                Última actualización: <t t-esc="equipo.fecha_ultima_actualizacion.strftime('%d/%m/%Y %H:%M')"/>
                                            </small>
                                        </t>
                                    </div>
                                    <div class="card-body">
                                        <div class="row text-center">
                                            <div class="col-md-4 mb-3 mb-md-0">
                                                <div class="counter-box p-3 border rounded">
                                                    <h6 class="text-muted mb-2">Blanco y Negro</h6>
                                                    <h2 class="mb-0 text-dark">
                                                        <t t-esc="'{:,}'.format(equipo.contador_bn or 0)"/>
                                                    </h2>
                                                </div>
                                            </div>
                                            <t t-if="equipo.tipo_maquina_id == 'color'">
                                                <div class="col-md-4 mb-3 mb-md-0">
                                                    <div class="counter-box p-3 border rounded">
                                                        <h6 class="text-muted mb-2">Color</h6>
                                                        <h2 class="mb-0 text-primary">
                                                            <t t-esc="'{:,}'.format(equipo.contador_color or 0)"/>
                                                        </h2>
                                                    </div>
                                                </div>
                                            </t>
                                            <div class="col-md-4">
                                                <div class="counter-box p-3 border rounded">
                                                    <h6 class="text-muted mb-2">Escáner</h6>
                                                    <h2 class="mb-0 text-info">
                                                        <t t-esc="'{:,}'.format(equipo.contador_scan or 0)"/>
                                                    </h2>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>

                    <!-- Botones de Acción -->
                    <div class="row mb-4">
                        <div class="col-12">
                            <div class="card shadow-sm">
                                <div class="card-header bg-warning text-dark">
                                    <i class="fa fa-tools"></i> Acciones Rápidas
                                </div>
                                <div class="card-body">
                                    <div class="row g-3">
                                        <div class="col-md-4">
                                            <a t-attf-href="/ticket/reportar_incidencia?id_registro=#{equipo.id}" 
                                               class="btn btn-danger w-100 btn-lg">
                                                <i class="fa fa-exclamation-circle"></i> Reportar Problema
                                            </a>
                                        </div>
                                        <div class="col-md-4">
                                            <a t-attf-href="/toner/solicitar_toner?id_registro=#{equipo.id}" 
                                               class="btn btn-primary w-100 btn-lg">
                                                <i class="fa fa-tint"></i> Solicitar Tóner
                                            </a>
                                        </div>
                                        <div class="col-md-4">
                                            <a t-attf-href="/alquiler/repuestos/#{equipo.id}" 
                                               class="btn btn-info w-100 btn-lg">
                                                <i class="fa fa-cogs"></i> Ver Repuestos
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Historial de Tickets -->
                    <div class="row mb-4">
                        <div class="col-12">
                            <div class="card shadow-sm">
                                <div class="card-header bg-secondary text-white d-flex justify-content-between align-items-center">
                                    <span>
                                        <i class="fa fa-ticket-alt"></i> Historial de Tickets
                                    </span>
                                    <span class="badge bg-light text-dark">
                                        <t t-esc="len(tickets)"/> tickets
                                    </span>
                                </div>
                                <div class="card-body p-0">
                                    <t t-if="not tickets">
                                        <div class="text-center py-4">
                                            <i class="fa fa-inbox fa-3x text-muted mb-3"></i>
                                            <p class="text-muted">No hay tickets registrados para este equipo</p>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <div class="table-responsive">
                                            <table class="table table-hover mb-0">
                                                <thead class="table-light">
                                                    <tr>
                                                        <th>Ticket</th>
                                                        <th>Fecha</th>
                                                        <th>Tipo de Servicio</th>
                                                        <th>Técnico</th>
                                                        <th>Estado</th>
                                                        <th class="text-end">Acción</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <t t-foreach="tickets" t-as="ticket">
                                                        <tr>
                                                            <td>
                                                                <strong><t t-esc="ticket.name"/></strong>
                                                            </td>
                                                            <td>
                                                                <t t-if="ticket.agenda">
                                                                    <t t-esc="ticket.agenda.strftime('%d/%m/%Y')"/>
                                                                </t>
                                                                <t t-else="">
                                                                    <span class="text-muted">Sin fecha</span>
                                                                </t>
                                                            </td>
                                                            <td>
                                                                <t t-if="ticket.tipo_servicio_id">
                                                                    <t t-esc="dict(ticket._fields['tipo_servicio_id'].selection).get(ticket.tipo_servicio_id, ticket.tipo_servicio_id)"/>
                                                                </t>
                                                            </td>
                                                            <td>
                                                                <t t-if="ticket.responsable">
                                                                    <t t-esc="ticket.responsable.name"/>
                                                                </t>
                                                                <t t-else="">
                                                                    <span class="text-muted">Sin asignar</span>
                                                                </t>
                                                            </td>
                                                            <td>
                                                                <t t-if="ticket.estado == 'nuevo'">
                                                                    <span class="badge bg-info">Nuevo</span>
                                                                </t>
                                                                <t t-elif="ticket.estado == 'proceso'">
                                                                    <span class="badge bg-warning text-dark">En Proceso</span>
                                                                </t>
                                                                <t t-elif="ticket.estado == 'finalizado'">
                                                                    <span class="badge bg-success">Finalizado</span>
                                                                </t>
                                                                <t t-else="">
                                                                    <span class="badge bg-secondary">
                                                                        <t t-esc="ticket.estado"/>
                                                                    </span>
                                                                </t>
                                                            </td>
                                                            <td class="text-end">
                                                                <a t-attf-href="/my/ticket/#{ticket.id}" 
                                                                   class="btn btn-sm btn-outline-primary">
                                                                    <i class="fa fa-eye"></i>
                                                                </a>
                                                            </td>
                                                        </tr>
                                                    </t>
                                                </tbody>
                                            </table>
                                        </div>
                                        <div class="card-footer text-center">
                                            <a href="/my/tickets" class="btn btn-link">
                                                Ver todos los tickets <i class="fa fa-arrow-right"></i>
                                            </a>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Historial de Contadores (si existen registros automáticos) -->
                    <t t-if="contadores">
                        <div class="row">
                            <div class="col-12">
                                <div class="card shadow-sm">
                                    <div class="card-header bg-primary text-white">
                                        <i class="fa fa-chart-line"></i> Historial de Contadores
                                    </div>
                                    <div class="card-body p-0">
                                        <div class="table-responsive">
                                            <table class="table table-sm table-hover mb-0">
                                                <thead class="table-light">
                                                    <tr>
                                                        <th>Fecha</th>
                                                        <th class="text-end">B/N</th>
                                                        <t t-if="equipo.tipo_maquina_id == 'color'">
                                                            <th class="text-end">Color</th>
                                                        </t>
                                                        <th class="text-end">Escáner</th>
                                                        <th>Fuente</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <t t-foreach="contadores" t-as="contador">
                                                        <tr>
                                                            <td>
                                                                <t t-esc="contador.fecha.strftime('%d/%m/%Y %H:%M')"/>
                                                            </td>
                                                            <td class="text-end">
                                                                <t t-esc="'{:,}'.format(contador.contador_bn or 0)"/>
                                                            </td>
                                                            <t t-if="equipo.tipo_maquina_id == 'color'">
                                                                <td class="text-end">
                                                                    <t t-esc="'{:,}'.format(contador.contador_color or 0)"/>
                                                                </td>
                                                            </t>
                                                            <td class="text-end">
                                                                <t t-esc="'{:,}'.format(contador.contador_scan or 0)"/>
                                                            </td>
                                                            <td>
                                                                <span class="badge bg-info">Automático</span>
                                                            </td>
                                                        </tr>
                                                    </t>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </div>
            </t>
        </template>

        <!-- ============================================ -->
        <!-- LISTA DE TICKETS -->
        <!-- ============================================ -->
        <template id="portal_my_tickets" name="Mis Tickets">
            <t t-call="portal.portal_layout">
                <t t-set="breadcrumbs_searchbar" t-value="True"/>

                <t t-call="portal.portal_searchbar">
                    <t t-set="title">Mis Tickets</t>
                </t>

                <div class="container mt-4">
                    <!-- Mensaje si no hay tickets -->
                    <t t-if="not tickets">
                        <div class="alert alert-info text-center" role="alert">
                            <i class="fa fa-info-circle fa-2x mb-3"></i>
                            <h4>No tiene tickets registrados</h4>
                            <p class="mb-3">Actualmente no tiene tickets de servicio técnico.</p>
                            <a href="/my/equipos" class="btn btn-primary">
                                <i class="fa fa-print"></i> Ver mis equipos
                            </a>
                        </div>
                    </t>

                    <!-- Lista de tickets -->
                    <t t-else="">
                        <t t-call="portal.portal_table">
                            <thead>
                                <tr class="active">
                                    <th>Ticket</th>
                                    <th>Equipo / Serie</th>
                                    <th>Fecha Visita</th>
                                    <th>Tipo de Servicio</th>
                                    <th>Técnico</th>
                                    <th>Estado</th>
                                    <th class="text-center">Acción</th>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-foreach="tickets" t-as="ticket">
                                    <tr>
                                        <td>
                                            <strong><t t-esc="ticket.name"/></strong>
                                            <br/>
                                            <small class="text-muted">
                                                <t t-esc="ticket.create_date.strftime('%d/%m/%Y')"/>
                                            </small>
                                        </td>
                                        <td>
                                            <t t-if="ticket.product_alquiler">
                                                <div><t t-esc="ticket.modelo_id_r"/></div>
                                                <small class="text-muted">
                                                    Serie: <t t-esc="ticket.serie_id_r"/>
                                                </small>
                                            </t>
                                            <t t-else="">
                                                <span class="text-muted">Sin equipo</span>
                                            </t>
                                        </td>
                                        <td>
                                            <t t-if="ticket.agenda">
                                                <t t-esc="ticket.agenda.strftime('%d/%m/%Y')"/>
                                                <br/>
                                                <small class="text-muted">
                                                    <t t-esc="ticket.agenda.strftime('%H:%M')"/>
                                                </small>
                                            </t>
                                            <t t-else="">
                                                <span class="text-muted">Sin programar</span>
                                            </t>
                                        </td>
                                        <td>
                                            <t t-if="ticket.tipo_servicio_id">
                                                <small>
                                                    <t t-esc="dict(ticket._fields['tipo_servicio_id'].selection).get(ticket.tipo_servicio_id, ticket.tipo_servicio_id)"/>
                                                </small>
                                            </t>
                                        </td>
                                        <td>
                                            <t t-if="ticket.responsable">
                                                <t t-esc="ticket.responsable.name"/>
                                            </t>
                                            <t t-else="">
                                                <span class="text-muted">Sin asignar</span>
                                            </t>
                                        </td>
                                        <td>
                                            <t t-if="ticket.estado == 'nuevo'">
                                                <span class="badge bg-info">Nuevo</span>
                                            </t>
                                            <t t-elif="ticket.estado == 'proceso'">
                                                <span class="badge bg-warning text-dark">En Proceso</span>
                                            </t>
                                            <t t-elif="ticket.estado == 'finalizado'">
                                                <span class="badge bg-success">Finalizado</span>
                                            </t>
                                            <t t-else="">
                                                <span class="badge bg-secondary">
                                                    <t t-esc="ticket.estado"/>
                                                </span>
                                            </t>
                                        </td>
                                        <td class="text-center">
                                            <a t-attf-href="/my/ticket/#{ticket.id}" 
                                               class="btn btn-sm btn-primary">
                                                <i class="fa fa-eye"></i> Ver
                                            </a>
                                        </td>
                                    </tr>
                                </t>
                            </tbody>
                        </t>
                    </t>
                </div>
            </t>
        </template>

        <!-- ============================================ -->
        <!-- DETALLE DE TICKET -->
        <!-- ============================================ -->
        <template id="portal_ticket_detail" name="Detalle de Ticket">
            <t t-call="portal.portal_layout">
                <t t-set="o_portal_fullwidth_alert" groups="base.group_portal">
                    <t t-call="portal.portal_back_in_edit_mode">
                        <t t-set="backend_url" t-value="'/web#model=ticket.alquiler&amp;id=%s&amp;view_type=form' % (ticket.id)"/>
                    </t>
                </t>

                <div class="container mt-4 mb-5">
                    <!-- Breadcrumb -->
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/my/home">Portal</a></li>
                            <li class="breadcrumb-item"><a href="/my/tickets">Mis Tickets</a></li>
                            <li class="breadcrumb-item active" aria-current="page">
                                <t t-esc="ticket.name"/>
                            </li>
                        </ol>
                    </nav>

                    <!-- Header del Ticket -->
                    <div class="row mb-4">
                        <div class="col-12">
                            <div class="card border-primary shadow">
                                <div class="card-body">
                                    <div class="row align-items-center">
                                        <div class="col-md-8">
                                            <h2 class="mb-2">
                                                <i class="fa fa-ticket-alt text-primary"></i>
                                                Ticket <t t-esc="ticket.name"/>
                                            </h2>
                                            <p class="text-muted mb-0">
                                                <strong>Creado:</strong>
                                                <t t-esc="ticket.create_date.strftime('%d/%m/%Y %H:%M')"/>
                                            </p>
                                        </div>
                                        <div class="col-md-4 text-md-end mt-3 mt-md-0">
                                            <t t-if="ticket.estado == 'nuevo'">
                                                <span class="badge bg-info fs-5">Nuevo</span>
                                            </t>
                                            <t t-elif="ticket.estado == 'proceso'">
                                                <span class="badge bg-warning text-dark fs-5">En Proceso</span>
                                            </t>
                                            <t t-elif="ticket.estado == 'finalizado'">
                                                <span class="badge bg-success fs-5">Finalizado</span>
                                            </t>
                                            <t t-else="">
                                                <span class="badge bg-secondary fs-5">
                                                    <t t-esc="ticket.estado"/>
                                                </span>
                                            </t>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Información del Servicio -->
                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <div class="card h-100 shadow-sm">
                                <div class="card-header bg-primary text-white">
                                    <i class="fa fa-info-circle"></i> Información del Servicio
                                </div>
                                <div class="card-body">
                                    <table class="table table-sm table-borderless">
                                        <tr>
                                            <td class="text-muted">Tipo de Servicio:</td>
                                            <td>
                                                <strong>
                                                    <t t-if="ticket.tipo_servicio_id">
                                                        <t t-esc="dict(ticket._fields['tipo_servicio_id'].selection).get(ticket.tipo_servicio_id, ticket.tipo_servicio_id)"/>
                                                    </t>
                                                </strong>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td class="text-muted">Fecha de Visita:</td>
                                            <td>
                                                <t t-if="ticket.agenda">
                                                    <strong><t t-esc="ticket.agenda.strftime('%d/%m/%Y %H:%M')"/></strong>
                                                </t>
                                                <t t-else="">
                                                    <span class="text-muted">Sin programar</span>
                                                </t>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td class="text-muted">Técnico Asignado:</td>
                                            <td>
                                                <t t-if="ticket.responsable">
                                                    <strong><t t-esc="ticket.responsable.name"/></strong>
                                                </t>
                                                <t t-else="">
                                                    <span class="text-muted">Sin asignar</span>
                                                </t>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td class="text-muted">Prioridad:</td>
                                            <td>
                                                <t t-if="ticket.priority == '3'">
                                                    <span class="badge bg-danger">Muy Alta</span>
                                                </t>
                                                <t t-elif="ticket.priority == '2'">
                                                    <span class="badge bg-warning text-dark">Alta</span>
                                                </t>
                                                <t t-elif="ticket.priority == '1'">
                                                    <span class="badge bg-info">Media</span>
                                                </t>
                                                <t t-else="">
                                                    <span class="badge bg-secondary">Baja</span>
                                                </t>
                                            </td>
                                        </tr>
                                    </table>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6 mb-3">
                            <div class="card h-100 shadow-sm">
                                <div class="card-header bg-info text-white">
                                    <i class="fa fa-print"></i> Equipo
                                </div>
                                <div class="card-body">
                                    <t t-if="ticket.product_alquiler">
                                        <table class="table table-sm table-borderless">
                                            <tr>
                                                <td class="text-muted">Modelo:</td>
                                                <td><strong><t t-esc="ticket.modelo_id_r"/></strong></td>
                                            </tr>
                                            <tr>
                                                <td class="text-muted">Marca:</td>
                                                <td><strong><t t-esc="ticket.marca_id_r"/></strong></td>
                                            </tr>
                                            <tr>
                                                <td class="text-muted">Serie:</td>
                                                <td>
                                                    <span class="badge bg-dark"><t t-esc="ticket.serie_id_r"/></span>
                                                </td>
                                            </tr>
                                            <tr>
                                                <td class="text-muted">Tipo:</td>
                                                <td>
                                                    <t t-if="ticket.tipo_id == 'color'">
                                                        <span class="badge bg-primary">Color</span>
                                                    </t>
                                                    <t t-else="">
                                                        <span class="badge bg-secondary">Monocromática</span>
                                                    </t>
                                                </td>
                                            </tr>
                                        </table>
                                        <div class="mt-3">
                                            <a t-attf-href="/my/equipo/#{ticket.product_alquiler.id}" 
                                               class="btn btn-sm btn-outline-primary w-100">
                                                <i class="fa fa-eye"></i> Ver equipo completo
                                            </a>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <p class="text-muted text-center py-4">
                                            No hay equipo asociado
                                        </p>
                                    </t>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Descripción del Problema -->
                    <t t-if="ticket.description">
                        <div class="row mb-4">
                            <div class="col-12">
                                <div class="card shadow-sm">
                                    <div class="card-header bg-warning text-dark">
                                        <i class="fa fa-exclamation-triangle"></i> Descripción del Problema
                                    </div>
                                    <div class="card-body">
                                        <p class="mb-0" style="white-space: pre-line;">
                                            <t t-esc="ticket.description"/>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>

                    <!-- Informe Técnico (si está finalizado) -->
                    <t t-if="ticket.estado == 'finalizado' and ticket.informe_id">
                        <div class="row mb-4">
                            <div class="col-12">
                                <div class="card shadow-sm">
                                    <div class="card-header bg-success text-white">
                                        <i class="fa fa-file-alt"></i> Informe Técnico
                                    </div>
                                    <div class="card-body">
                                        <t t-raw="ticket.informe_id"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>

                    <!-- Contadores registrados (si existen) -->
                    <t t-if="ticket.estado == 'finalizado' and (ticket.contometrok_id or ticket.contometroc_id)">
                        <div class="row">
                            <div class="col-12">
                                <div class="card shadow-sm">
                                    <div class="card-header bg-secondary text-white">
                                        <i class="fa fa-tachometer-alt"></i> Contadores Registrados
                                    </div>
                                    <div class="card-body">
                                        <div class="row text-center">
                                            <div class="col-md-4">
                                                <div class="p-3">
                                                    <h6 class="text-muted">Blanco y Negro</h6>
                                                    <h3 class="mb-0">
                                                        <t t-esc="'{:,}'.format(int(ticket.contometrok_id) if ticket.contometrok_id else 0)"/>
                                                    </h3>
                                                </div>
                                            </div>
                                            <t t-if="ticket.tipo_id == 'color'">
                                                <div class="col-md-4">
                                                    <div class="p-3">
                                                        <h6 class="text-muted">Color</h6>
                                                        <h3 class="mb-0 text-primary">
                                                            <t t-esc="'{:,}'.format(int(ticket.contometroc_id) if ticket.contometroc_id else 0)"/>
                                                        </h3>
                                                    </div>
                                                </div>
                                            </t>
                                            <div class="col-md-4">
                                                <div class="p-3">
                                                    <h6 class="text-muted">Escáner</h6>
                                                    <h3 class="mb-0 text-info">
                                                        <t t-esc="'{:,}'.format(int(ticket.contometros_id) if ticket.contometros_id else 0)"/>
                                                    </h3>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </div>
            </t>
        </template>
        <template id="portal_sat_custom_css" name="Portal SAT Custom CSS" inherit_id="website.layout">
            <xpath expr="//head" position="inside">
                <style type="text/css">
                    /* Efectos hover para cards */
                    .hover-card {
                        transition: transform 0.3s ease, box-shadow 0.3s ease;
                    }
                    .hover-card:hover {
                        transform: translateY(-5px);
                        box-shadow: 0 8px 16px rgba(0,0,0,0.15) !important;
                    }

                    /* Counter boxes */
                    .counter-box {
                        background-color: #f8f9fa;
                        transition: background-color 0.3s ease;
                    }
                    .counter-box:hover {
                        background-color: #e9ecef;
                    }

                    /* Badges personalizados */
                    .badge {
                        font-weight: 500;
                        padding: 0.35em 0.65em;
                    }

                    /* Table responsive improvements */
                    .table-responsive {
                        border-radius: 0.375rem;
                    }

                    /* Card headers */
                    .card-header {
                        font-weight: 600;
                    }

                    /* Breadcrumb styling */
                    .breadcrumb {
                        background-color: transparent;
                        padding: 0;
                    }

                    /* Alert improvements */
                    .alert i.fa {
                        margin-right: 0.5rem;
                    }

                    /* Button improvements for actions */
                    .btn-lg {
                        padding: 0.75rem 1.25rem;
                        font-size: 1rem;
                    }

                    /* Status indicators */
                    .status-indicator {
                        width: 10px;
                        height: 10px;
                        border-radius: 50%;
                        display: inline-block;
                        margin-right: 5px;
                    }
                    .status-nuevo { background-color: #17a2b8; }
                    .status-proceso { background-color: #ffc107; }
                    .status-finalizado { background-color: #28a745; }

                    /* Mobile responsiveness */
                    @media (max-width: 768px) {
                        .card-body {
                            padding: 1rem;
                        }
                        h2 {
                            font-size: 1.5rem;
                        }
                        .btn-lg {
                            padding: 0.5rem 1rem;
                            font-size: 0.9rem;
                        }
                    }

                    /* Loading animation */
                    @keyframes pulse {
                        0% { opacity: 1; }
                        50% { opacity: 0.5; }
                        100% { opacity: 1; }
                    }
                    .loading-pulse {
                        animation: pulse 1.5s ease-in-out infinite;
                    }
                </style>
            </xpath>
        </template>

    </data>
</odoo>
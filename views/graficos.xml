<!-- En tu módulo, por ejemplo en my_module/views/grafico_template.xml -->
<odoo>
    <template id="template_graficos_dinamicos" name="Gráficos Dinámicos">
        <div>
            <form id="form_grafico">
                <label for="modelo">Modelo:</label>
                <input id="modelo" name="modelo" type="text" placeholder="Ej. res.partner"/>
                <label for="fecha_inicio">Fecha Inicio:</label>
                <input id="fecha_inicio" name="fecha_inicio" type="date"/>
                <label for="fecha_fin">Fecha Fin:</label>
                <input id="fecha_fin" name="fecha_fin" type="date"/>
                <label for="campos">Campo:</label>
                <input id="campos" name="campos" type="text" placeholder="Ej. name"/>
                <button type="button" onclick="configurarYObtenerDatos()">Configurar y Ver Gráfico</button>
            </form>
            <canvas id="myChart"></canvas>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
        <script type="text/javascript">
            async function configurarYObtenerDatos() {
                const form = document.getElementById('form_grafico');
                const formData = new FormData(form);
                const data = {
                    modelo: formData.get('modelo'),
                    fecha_inicio: formData.get('fecha_inicio'),
                    fecha_fin: formData.get('fecha_fin'),
                    campos: formData.get('campos').split(',')
                };

                const response = await fetch('/api/grafico/datos', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify(data)
                });
                const result = await response.json();
                if (result.error) {
                    console.error(result.error);
                } else {
                    const ctx = document.getElementById('myChart').getContext('2d');
                    new Chart(ctx, {
                        type: 'bar', // Cambiar según necesidad
                        data: {
                            labels: result.labels,
                            datasets: result.datasets
                        },
                        options: {}
                    });
                }
            }
        </script>
    </template>
</odoo>

<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data>
    <!-- Template principal del dashboard -->
    <template id="equipment_blocking_dashboard_template" name="Dashboard Bloqueo de Equipos">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-12">
                            <!-- Header -->
                            <div class="card mt-4 mb-4 border-0 shadow-sm">
                                <div class="card-body">
                                    <div class="d-flex align-items-center">
                                        <i class="fas fa-shield-alt fa-2x text-primary me-3"></i>
                                        <div>
                                            <h1 class="h2 mb-1 fw-bold">Sistema de Bloqueo de Equipos</h1>
                                            <p class="text-muted mb-0">Gestión centralizada de estados de servicio y bloqueos remotos</p>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Dashboard Cards -->
                            <div class="row g-3 mb-4">
                                <div class="col-xl-2 col-lg-4 col-md-6">
                                    <div class="card border-0 shadow-sm h-100 dashboard-card">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="text-muted text-uppercase fw-bold small mb-2">Equipos Activos</h6>
                                                    <h2 class="h1 fw-bold mb-0" id="equipos-activos">
                                                        <t t-esc="dashboard_data.get('equipos_activos', 0)"/>
                                                    </h2>
                                                    <small class="text-success">
                                                        <i class="fas fa-arrow-up"></i> Funcionando normal
                                                    </small>
                                                </div>
                                                <div class="bg-success rounded-circle p-3">
                                                    <i class="fas fa-check-circle text-white fa-lg"></i>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="card-footer bg-success" style="height: 4px; padding: 0;"></div>
                                    </div>
                                </div>

                                <div class="col-xl-2 col-lg-4 col-md-6">
                                    <div class="card border-0 shadow-sm h-100 dashboard-card">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="text-muted text-uppercase fw-bold small mb-2">Suspendidos</h6>
                                                    <h2 class="h1 fw-bold mb-0" id="equipos-suspendidos">
                                                        <t t-esc="dashboard_data.get('equipos_suspendidos', 0)"/>
                                                    </h2>
                                                    <small class="text-warning">
                                                        <i class="fas fa-exclamation-triangle"></i> Por mora
                                                    </small>
                                                </div>
                                                <div class="bg-warning rounded-circle p-3">
                                                    <i class="fas fa-pause-circle text-white fa-lg"></i>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="card-footer bg-warning" style="height: 4px; padding: 0;"></div>
                                    </div>
                                </div>

                                <div class="col-xl-2 col-lg-4 col-md-6">
                                    <div class="card border-0 shadow-sm h-100 dashboard-card">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="text-muted text-uppercase fw-bold small mb-2">Bloqueados</h6>
                                                    <h2 class="h1 fw-bold mb-0" id="equipos-bloqueados">
                                                        <t t-esc="dashboard_data.get('equipos_bloqueados', 0)"/>
                                                    </h2>
                                                    <small class="text-danger">
                                                        <i class="fas fa-ban"></i> Acceso denegado
                                                    </small>
                                                </div>
                                                <div class="bg-danger rounded-circle p-3">
                                                    <i class="fas fa-lock text-white fa-lg"></i>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="card-footer bg-danger" style="height: 4px; padding: 0;"></div>
                                    </div>
                                </div>

                                <div class="col-xl-2 col-lg-4 col-md-6">
                                    <div class="card border-0 shadow-sm h-100 dashboard-card">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="text-muted text-uppercase fw-bold small mb-2">No Accesibles</h6>
                                                    <h2 class="h1 fw-bold mb-0" id="equipos-no-accesibles">
                                                        <t t-esc="dashboard_data.get('equipos_no_accesibles', 0)"/>
                                                    </h2>
                                                    <small class="text-muted">
                                                        <i class="fas fa-tools"></i> Requiere manual
                                                    </small>
                                                </div>
                                                <div class="bg-secondary rounded-circle p-3">
                                                    <i class="fas fa-wifi-slash text-white fa-lg"></i>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="card-footer bg-secondary" style="height: 4px; padding: 0;"></div>
                                    </div>
                                </div>

                                <div class="col-xl-2 col-lg-4 col-md-6">
                                    <div class="card border-0 shadow-sm h-100 dashboard-card">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="text-muted text-uppercase fw-bold small mb-2">Pend. Bloqueo</h6>
                                                    <h2 class="h1 fw-bold mb-0" id="pendientes-bloqueo">
                                                        <t t-esc="dashboard_data.get('pendientes_bloqueo', 0)"/>
                                                    </h2>
                                                    <small class="text-info">
                                                        <i class="fas fa-hourglass-half"></i> En proceso
                                                    </small>
                                                </div>
                                                <div class="bg-info rounded-circle p-3">
                                                    <i class="fas fa-clock text-white fa-lg"></i>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="card-footer bg-info" style="height: 4px; padding: 0;"></div>
                                    </div>
                                </div>

                                <div class="col-xl-2 col-lg-4 col-md-6">
                                    <div class="card border-0 shadow-sm h-100 dashboard-card">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="text-muted text-uppercase fw-bold small mb-2">Pend. Desbloqueo</h6>
                                                    <h2 class="h1 fw-bold mb-0" id="pendientes-desbloqueo">
                                                        <t t-esc="dashboard_data.get('pendientes_desbloqueo', 0)"/>
                                                    </h2>
                                                    <small class="text-primary">
                                                        <i class="fas fa-sync-alt"></i> Restaurando
                                                    </small>
                                                </div>
                                                <div class="bg-primary rounded-circle p-3">
                                                    <i class="fas fa-unlock-alt text-white fa-lg"></i>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="card-footer bg-primary" style="height: 4px; padding: 0;"></div>
                                    </div>
                                </div>
                            </div>

                            <!-- Search Section -->
                            <div class="card mb-4 border-0 shadow-sm">
                                <div class="card-body">
                                    <div class="row g-3 align-items-end">
                                        <div class="col-lg-6">
                                            <label for="search-input" class="form-label fw-bold">
                                                <i class="fas fa-search me-2"></i>Buscar Equipos
                                            </label>
                                            <input type="text" id="search-input" class="form-control form-control-lg" 
                                                   placeholder="Buscar por serie, cliente, modelo o marca..." />
                                        </div>
                                        <div class="col-lg-6">
                                            <div class="d-flex gap-2 flex-wrap">
                                                <button type="button" class="btn btn-primary btn-lg" onclick="searchEquipments()">
                                                    <i class="fas fa-search me-2"></i>Buscar
                                                </button>
                                                <button type="button" class="btn btn-warning btn-lg" onclick="loadPendingEquipments()">
                                                    <i class="fas fa-exclamation-circle me-2"></i>Ver Pendientes
                                                </button>
                                                <button type="button" class="btn btn-info btn-lg" onclick="refreshDashboard()">
                                                    <i class="fas fa-sync-alt me-2"></i>Actualizar
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Results Section -->
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h4 class="mb-0">
                                        <i class="fas fa-list me-2"></i>Resultados de Búsqueda
                                    </h4>
                                </div>
                                <div class="card-body" id="equipment-results">
                                    <div class="text-center py-5">
                                        <i class="fas fa-search fa-3x text-muted mb-3"></i>
                                        <p class="text-muted">Utiliza el buscador para encontrar equipos o haz clic en "Ver Pendientes" para equipos que requieren atención.</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Modal para acciones -->
            <div class="modal fade" id="actionModal" tabindex="-1" aria-hidden="true">
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="modalTitle">Acción en Equipo</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            <div id="modalAlert"></div>
                            <form id="actionForm">
                                <div class="mb-3">
                                    <label for="motivoInput" class="form-label fw-bold">Motivo / Observaciones:</label>
                                    <textarea class="form-control" id="motivoInput" rows="3" 
                                              placeholder="Describe el motivo de esta acción..."></textarea>
                                </div>
                                <div class="mb-3" id="equipmentDetails" style="display: none;">
                                    <div class="border rounded p-3 bg-light">
                                        <h6 class="fw-bold mb-2">Detalles del Equipo:</h6>
                                        <div class="row">
                                            <div class="col-md-6">
                                                <small class="text-muted">Cliente:</small>
                                                <div id="detailCliente" class="fw-bold"></div>
                                            </div>
                                            <div class="col-md-6">
                                                <small class="text-muted">Modelo:</small>
                                                <div id="detailModelo" class="fw-bold"></div>
                                            </div>
                                            <div class="col-md-6 mt-2">
                                                <small class="text-muted">IP:</small>
                                                <div id="detailIP"></div>
                                            </div>
                                            <div class="col-md-6 mt-2">
                                                <small class="text-muted">Estado Actual:</small>
                                                <div id="detailEstado"></div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                            <button type="button" class="btn btn-primary" id="confirmActionBtn">
                                <span id="btnLoading" class="d-none">
                                    <span class="spinner-border spinner-border-sm me-2" role="status"></span>
                                    Procesando...
                                </span>
                                <span id="btnText">Confirmar</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <!-- Template de error -->
    <template id="equipment_blocking_error_template" name="Error Template">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container mt-5">
                    <div class="row justify-content-center">
                        <div class="col-md-6">
                            <div class="alert alert-danger text-center">
                                <i class="fas fa-exclamation-triangle fa-3x mb-3"></i>
                                <h4>Error</h4>
                                <p t-esc="error_message"/>
                                <a href="/equipment/blocking/dashboard" class="btn btn-primary">
                                    <i class="fas fa-arrow-left me-2"></i>Volver al Dashboard
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
    <!-- CSS personalizado -->
            <style>
                .dashboard-card {
                    transition: transform 0.2s ease, box-shadow 0.2s ease;
                    cursor: pointer;
                }
                
                .dashboard-card:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !important;
                }
                
                .equipment-card {
                    border: 1px solid #dee2e6;
                    border-radius: 0.5rem;
                    padding: 1.5rem;
                    margin-bottom: 1rem;
                    transition: all 0.2s ease;
                    background: white;
                }
                
                .equipment-card:hover {
                    border-color: #0d6efd;
                    box-shadow: 0 0.125rem 0.25rem rgba(13, 110, 253, 0.1);
                    transform: translateY(-1px);
                }
                
                .status-badge {
                    padding: 0.5rem 1rem;
                    border-radius: 2rem;
                    font-weight: 600;
                    font-size: 0.875rem;
                    text-transform: uppercase;
                    letter-spacing: 0.025em;
                }
                
                .status-activo {
                    background-color: #d1ecf1;
                    color: #0c5460;
                    border: 1px solid #bee5eb;
                }
                
                .status-suspendido {
                    background-color: #fff3cd;
                    color: #856404;
                    border: 1px solid #ffeaa7;
                }
                
                .status-bloqueado {
                    background-color: #f8d7da;
                    color: #721c24;
                    border: 1px solid #f5c6cb;
                }
                
                .status-no_accesible {
                    background-color: #e2e3e5;
                    color: #383d41;
                    border: 1px solid #d6d8db;
                }
                
                .status-pendiente_bloqueo,
                .status-pendiente_desbloqueo {
                    background-color: #d4edda;
                    color: #155724;
                    border: 1px solid #c3e6cb;
                }
                
                .detail-grid {
                    display: grid;
                    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                    gap: 1rem;
                    margin: 1rem 0;
                }
                
                .detail-item {
                    display: flex;
                    align-items: center;
                    gap: 0.5rem;
                    font-size: 0.9rem;
                }
                
                .detail-item i {
                    color: #6c757d;
                    width: 16px;
                    text-align: center;
                }
                
                @media (max-width: 768px) {
                    .detail-grid {
                        grid-template-columns: 1fr;
                        gap: 0.5rem;
                    }
                }
            </style>

            <!-- JavaScript -->
            <script type="text/javascript">
                odoo.define('equipment_blocking_dashboard', function (require) {
                    'use strict';

                    var core = require('web.core');
                    var publicWidget = require('web.public.widget');
                    var ajax = require('web.ajax');

                    var EquipmentBlockingDashboard = publicWidget.Widget.extend({
                        selector: '#wrap',
                        events: {
                            'click .dashboard-card': '_onDashboardCardClick',
                            'keypress #search-input': '_onSearchKeypress',
                            'click #confirmActionBtn': '_executeAction',
                        },

                        init: function () {
                            this._super.apply(this, arguments);
                            this.currentAction = null;
                            this.currentEquipmentId = null;
                            this.actionModal = null;
                        },

                        start: function () {
                            var self = this;
                            return this._super.apply(this, arguments).then(function () {
                                // Inicializar modal de Bootstrap
                                var modalEl = document.getElementById('actionModal');
                                if (modalEl &amp;&amp; typeof bootstrap !== 'undefined') {
                                    self.actionModal = new bootstrap.Modal(modalEl);
                                }
                                
                                // Configurar event listeners
                                self._setupEventListeners();
                            });
                        },

                        _setupEventListeners: function () {
                            var self = this;
                            
                            // Shortcuts de teclado
                            document.addEventListener('keydown', function(e) {
                                // Ctrl/Cmd + K para buscar
                                if ((e.ctrlKey || e.metaKey) &amp;&amp; e.key === 'k') {
                                    e.preventDefault();
                                    document.getElementById('search-input').focus();
                                }
                                
                                // Escape para cerrar modal
                                if (e.key === 'Escape' &amp;&amp; self.actionModal) {
                                    self.actionModal.hide();
                                }
                            });
                        },

                        _onSearchKeypress: function (ev) {
                            if (ev.which === 13) { // Enter key
                                this.searchEquipments();
                            }
                        },

                        _onDashboardCardClick: function (ev) {
                            // Implementar filtro por tipo de estado si es necesario
                        },

                        getCsrfToken: function () {
                            return odoo.csrf_token || $('meta[name="csrf-token"]').attr('content') || '';
                        },

                        refreshDashboard: function () {
                            var self = this;
                            return ajax.jsonRpc('/equipment/blocking/dashboard_data', 'call', {})
                                .then(function (result) {
                                    if (result.status === 'success') {
                                        var data = result.data;
                                        $('#equipos-activos').text(data.equipos_activos);
                                        $('#equipos-suspendidos').text(data.equipos_suspendidos);
                                        $('#equipos-bloqueados').text(data.equipos_bloqueados);
                                        $('#equipos-no-accesibles').text(data.equipos_no_accesibles);
                                        $('#pendientes-bloqueo').text(data.pendientes_bloqueo);
                                        $('#pendientes-desbloqueo').text(data.pendientes_desbloqueo);
                                        
                                        self.showAlert('success', 'Dashboard actualizado correctamente');
                                    }
                                })
                                .fail(function (error) {
                                    console.error('Error al actualizar dashboard:', error);
                                    self.showAlert('danger', 'Error al actualizar el dashboard');
                                });
                        },

                        searchEquipments: function () {
                            var self = this;
                            var searchTerm = $('#search-input').val();
                            var resultsContainer = $('#equipment-results');
                            
                            resultsContainer.html(`
                                <div class="text-center py-5">
                                    <div class="spinner-border text-primary mb-3" role="status"></div>
                                    <p class="text-muted">Buscando equipos...</p>
                                </div>
                            `);

                            return ajax.jsonRpc('/equipment/blocking/search', 'call', {
                                search_term: searchTerm
                            }).then(function (result) {
                                if (result.status === 'success') {
                                    self.displayEquipments(result.equipos);
                                } else {
                                    self.showAlert('danger', 'Error al buscar equipos: ' + result.message);
                                }
                            }).fail(function (error) {
                                self.showAlert('danger', 'Error de conexión');
                                console.error('Error:', error);
                            });
                        },

                        loadPendingEquipments: function () {
                            var self = this;
                            $('#search-input').val('');
                            
                            var resultsContainer = $('#equipment-results');
                            resultsContainer.html(`
                                <div class="text-center py-5">
                                    <div class="spinner-border text-warning mb-3" role="status"></div>
                                    <p class="text-muted">Cargando equipos pendientes...</p>
                                </div>
                            `);

                            return ajax.jsonRpc('/equipment/blocking/search', 'call', {
                                search_term: '',
                                only_pending: true
                            }).then(function (result) {
                                if (result.status === 'success') {
                                    var pendingEquipments = result.equipos.filter(function(eq) {
                                        return ['suspendido', 'bloqueado', 'no_accesible', 'pendiente_bloqueo', 'pendiente_desbloqueo'].includes(eq.estado_bloqueo);
                                    });
                                    self.displayEquipments(pendingEquipments);
                                } else {
                                    self.showAlert('danger', 'Error al cargar equipos: ' + result.message);
                                }
                            }).fail(function (error) {
                                self.showAlert('danger', 'Error de conexión');
                                console.error('Error:', error);
                            });
                        },

                        displayEquipments: function (equipos) {
                            var self = this;
                            var resultsContainer = $('#equipment-results');
                            
                            if (equipos.length === 0) {
                                resultsContainer.html(`
                                    <div class="text-center py-5">
                                        <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                                        <p class="text-muted">No se encontraron equipos con los criterios especificados.</p>
                                    </div>
                                `);
                                return;
                            }

                            var equipmentCards = equipos.map(function(equipo) {
                                var motivoSection = equipo.motivo_bloqueo ? 
                                    `<div class="alert alert-info py-2 mb-3">
                                        <strong>Motivo:</strong> ${self._escapeHtml(equipo.motivo_bloqueo)}
                                    </div>` : '';
                                
                                var fechaSection = equipo.fecha_bloqueo ? 
                                    `<div class="text-muted small mb-3">
                                        <i class="fas fa-clock"></i> ${self._escapeHtml(equipo.fecha_bloqueo)}
                                    </div>` : '';

                                var buttons = [];
                                if (equipo.puede_suspender) {
                                    buttons.push(`<button class="btn btn-warning btn-sm" onclick="equipmentDashboard.openActionModal('suspend', ${equipo.id}, '${self._escapeHtml(equipo.serie)}', ${JSON.stringify(equipo)})">
                                        <i class="fas fa-pause me-1"></i> Suspender
                                    </button>`);
                                }
                                if (equipo.puede_bloquear) {
                                    buttons.push(`<button class="btn btn-danger btn-sm" onclick="equipmentDashboard.openActionModal('block', ${equipo.id}, '${self._escapeHtml(equipo.serie)}', ${JSON.stringify(equipo)})">
                                        <i class="fas fa-lock me-1"></i> Bloquear
                                    </button>`);
                                }
                                if (equipo.puede_desbloquear) {
                                    buttons.push(`<button class="btn btn-success btn-sm" onclick="equipmentDashboard.openActionModal('unblock', ${equipo.id}, '${self._escapeHtml(equipo.serie)}', ${JSON.stringify(equipo)})">
                                        <i class="fas fa-unlock me-1"></i> Desbloquear
                                    </button>`);
                                }

                                return `
                                    <div class="equipment-card">
                                        <div class="d-flex justify-content-between align-items-start mb-3">
                                            <div>
                                                <h5 class="mb-1 fw-bold">Serie: ${self._escapeHtml(equipo.serie)}</h5>
                                                <p class="text-muted mb-0">${self._escapeHtml(equipo.cliente)} - ${self._escapeHtml(equipo.modelo)}</p>
                                            </div>
                                            <span class="status-badge status-${equipo.estado_bloqueo}">
                                                ${self._escapeHtml(equipo.estado_label)}
                                            </span>
                                        </div>
                                        
                                        <div class="detail-grid">
                                            <div class="detail-item">
                                                <i class="fas fa-industry"></i>
                                                <span>Marca: ${self._escapeHtml(equipo.marca)}</span>
                                            </div>
                                            <div class="detail-item">
                                                <i class="fas fa-map-marker-alt"></i>
                                                <span>Dirección: ${self._escapeHtml(equipo.direccion)}</span>
                                            </div>
                                            <div class="detail-item">
                                                <i class="fas fa-network-wired"></i>
                                                <span>IP: ${self._escapeHtml(equipo.ip_equipo || 'No configurada')}</span>
                                            </div>
                                            <div class="detail-item">
                                                <i class="fas fa-wifi"></i>
                                                <span>Acceso remoto: ${equipo.acceso_remoto ? 'Disponible' : 'No disponible'}</span>
                                            </div>
                                        </div>
                                        
                                        ${motivoSection}
                                        ${fechaSection}
                                        
                                        <div class="d-flex gap-2 justify-content-end flex-wrap">
                                            ${buttons.join('')}
                                        </div>
                                    </div>
                                `;
                            }).join('');

                            resultsContainer.html(equipmentCards);
                        },

                        openActionModal: function (action, equipmentId, serie, equipmentData) {
                            var self = this;
                            this.currentAction = action;
                            this.currentEquipmentId = equipmentId;
                            
                            var equipment = typeof equipmentData === 'string' ? JSON.parse(equipmentData) : equipmentData;
                            
                            var modalTitle = $('#modalTitle');
                            var motivoInput = $('#motivoInput');
                            var confirmBtn = $('#confirmActionBtn');
                            var equipmentDetails = $('#equipmentDetails');
                            
                            // Mostrar detalles del equipo
                            $('#detailCliente').text(equipment.cliente);
                            $('#detailModelo').text(equipment.modelo);
                            $('#detailIP').text(equipment.ip_equipo || 'No configurada');
                            $('#detailEstado').html(`<span class="status-badge status-${equipment.estado_bloqueo}">${equipment.estado_label}</span>`);
                            equipmentDetails.show();
                            
                            // Configurar según la acción
                            switch(action) {
                                case 'suspend':
                                    modalTitle.text(`Suspender Servicio - Serie: ${serie}`);
                                    motivoInput.attr('placeholder', 'Motivo de la suspensión (ej: Mora de pagos, incumplimiento contractual...)');
                                    confirmBtn.removeClass().addClass('btn btn-warning');
                                    break;
                                case 'block':
                                    modalTitle.text(`Bloquear Equipo - Serie: ${serie}`);
                                    motivoInput.attr('placeholder', 'Motivo del bloqueo remoto...');
                                    confirmBtn.removeClass().addClass('btn btn-danger');
                                    break;
                                case 'unblock':
                                    modalTitle.text(`Desbloquear Equipo - Serie: ${serie}`);
                                    motivoInput.attr('placeholder', 'Observaciones del desbloqueo...');
                                    confirmBtn.removeClass().addClass('btn btn-success');
                                    break;
                            }
                            
                            // Limpiar modal y mostrar
                            motivoInput.val('');
                            $('#modalAlert').html('');
                            
                            if (this.actionModal) {
                                this.actionModal.show();
                            }
                        },

                        _executeAction: function () {
                            var self = this;
                            var motivo = $('#motivoInput').val();
                            var btnLoading = $('#btnLoading');
                            var btnText = $('#btnText');
                            var confirmBtn = $('#confirmActionBtn');
                            
                            // Mostrar loading
                            btnLoading.removeClass('d-none');
                            btnText.addClass('d-none');
                            confirmBtn.prop('disabled', true);

                            var endpoints = {
                                'suspend': '/equipment/blocking/suspend',
                                'block': '/equipment/blocking/block',
                                'unblock': '/equipment/blocking/unblock'
                            };

                            return ajax.jsonRpc(endpoints[this.currentAction], 'call', {
                                equipment_id: this.currentEquipmentId,
                                motivo: motivo
                            }).then(function (result) {
                                if (result.status === 'success') {
                                    self.showModalAlert('success', result.message);
                                    
                                    // Cerrar modal después de 2 segundos y actualizar
                                    setTimeout(function() {
                                        if (self.actionModal) {
                                            self.actionModal.hide();
                                        }
                                        self.refreshDashboard();
                                        self.searchEquipments();
                                    }, 2000);
                                } else {
                                    self.showModalAlert('danger', result.message);
                                }
                            }).fail(function (error) {
                                self.showModalAlert('danger', 'Error de conexión');
                                console.error('Error:', error);
                            }).always(function () {
                                // Ocultar loading
                                btnLoading.addClass('d-none');
                                btnText.removeClass('d-none');
                                confirmBtn.prop('disabled', false);
                            });
                        },

                        showModalAlert: function (type, message) {
                            var alertContainer = $('#modalAlert');
                            var alertClass = type === 'success' ? 'alert-success' : 'alert-danger';
                            var icon = type === 'success' ? 'check-circle' : 'exclamation-triangle';
                            
                            alertContainer.html(`
                                <div class="alert ${alertClass} d-flex align-items-center" role="alert">
                                    <i class="fas fa-${icon} me-2"></i>
                                    <div>${this._escapeHtml(message)}</div>
                                </div>
                            `);
                        },

                        showAlert: function (type, message) {
                            var self = this;
                            // Crear contenedor de alertas si no existe
                            var alertContainer = $('#general-alerts');
                            if (alertContainer.length === 0) {
                                alertContainer = $('<div id="general-alerts" class="position-fixed top-0 end-0 p-3" style="z-index: 1055;"></div>');
                                $('body').append(alertContainer);
                            }
                            
                            var alertId = 'alert-' + Date.now();
                            var alertClass = type === 'success' ? 'alert-success' : 
                                           type === 'warning' ? 'alert-warning' : 'alert-danger';
                            var icon = type === 'success' ? 'check-circle' : 
                                      type === 'warning' ? 'exclamation-triangle' : 'times-circle';
                            
                            var alertHtml = `
                                <div id="${alertId}" class="alert ${alertClass} alert-dismissible fade show" role="alert">
                                    <i class="fas fa-${icon} me-2"></i>
                                    ${this._escapeHtml(message)}
                                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                </div>
                            `;
                            
                            alertContainer.append(alertHtml);
                            
                            // Auto-dismiss después de 5 segundos
                            setTimeout(function() {
                                var alert = $('#' + alertId);
                                if (alert.length &amp;&amp; typeof bootstrap !== 'undefined') {
                                    var bsAlert = new bootstrap.Alert(alert[0]);
                                    bsAlert.close();
                                }
                            }, 5000);
                        },

                        _escapeHtml: function (text) {
                            if (!text) return '';
                            var map = {
                                '&amp;&amp;': '&amp;',
                                '&lt;': '&lt;',
                                '&gt;': '&gt;',
                                '&quot;': '&quot;',
                                "&apos;": '&#039;'
                            };
                            return String(text).replace(/[&amp;&lt;&gt;"']/g, function(m) { return map[m]; });
                        },

                        filterByStatus: function (status) {
                            $('#search-input').val('');
                            this.loadPendingEquipments();
                        },

                        getEquipmentDetails: function (equipmentId) {
                            return ajax.jsonRpc('/equipment/blocking/get_equipment_details', 'call', {
                                equipment_id: equipmentId
                            }).then(function (result) {
                                return result.status === 'success' ? result.equipment : null;
                            }).fail(function (error) {
                                console.error('Error al obtener detalles:', error);
                                return null;
                            });
                        }
                    });

                    // Instanciar y exponer globalmente para compatibilidad
                    var equipmentDashboard = new EquipmentBlockingDashboard();
                    
                    // Funciones globales para compatibilidad con onclick
                    window.searchEquipments = function() {
                        equipmentDashboard.searchEquipments();
                    };
                    
                    window.loadPendingEquipments = function() {
                        equipmentDashboard.loadPendingEquipments();
                    };
                    
                    window.refreshDashboard = function() {
                        equipmentDashboard.refreshDashboard();
                    };
                    
                    window.equipmentDashboard = equipmentDashboard;

                    // Inicializar cuando el DOM esté listo
                    $(document).ready(function() {
                        equipmentDashboard.attachTo($('body'));
                    });

                    return EquipmentBlockingDashboard;
                });
            </script>

   </data>
</odoo>
<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="sat.EstadoVentasBar" owl="1">
        <div class="estado-ventas-bar" t-att-id="props.name">
            <div class="estado-options">
                <t t-foreach="estados" t-as="estado" t-key="estado.value">
                    <div t-att-class="getEstadoClass(estado.value)"
                         t-on-click.prevent="() => onEstadoClick(estado.value)">
                        <t t-esc="estado.label"/>
                    </div>
                </t>
            </div>
        </div>
    </t>
</templates>